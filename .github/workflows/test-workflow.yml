name: Test Workflow

on: workflow_dispatch

jobs:
  test-job:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Print Hello
      run: echo "Hello, world!"
